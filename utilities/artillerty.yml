config:
  #target: "http://localhost:3000"
  target: "http://stylishbe.xyz"
  http:
    pool: 20
    timeout: 3
  phases:
    - duration: 60
      arrivalRate: 150
      name: Read
scenarios:
  - flow:
      - loop:
          # - log: "Testing {{ $loopElement }}"
          - get:
              url: "/{{ $loopElement }}"
        over:
          - 1yaH3kT
          - whpndyH
          - RzX6amh
          - i3xR2OZ
