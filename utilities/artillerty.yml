config:
  #target: "http://localhost:3000"
  target: "http://stylishbe.xyz"
  http:
    pool: 20
    timeout: 3
  phases:
    - duration: 60
      arrivalRate: 160
      name: Read
scenarios:
  - flow:
      - loop:
          # - log: "Testing {{ $loopElement }}"
          - get:
              url: "/{{ $loopElement }}"
        over:
          - 1yaH3kT
          - i3xR2OZ
          - J27abqg
          - RzX6amh
